# =============================================================
# Monitors
# =============================================================
# Format: monitor=<name>,<resolution@hz>,<position>,<scale>
# Use "preferred" for auto-resolution, "auto" for auto-position.
# Example for explicit setup: monitor=DP-1,2560x1440@144,0x0,1

monitor = ,preferred,auto,auto
