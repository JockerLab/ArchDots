# =============================================================
# Environment Variables
# =============================================================
# Cursor size applies to both XCursor (X11 apps) and HyprCursor (native).

env = XCURSOR_SIZE,22
env = HYPRCURSOR_SIZE,22
