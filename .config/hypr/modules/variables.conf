# =============================================================
# Variables — application shortcuts and modifier key
# =============================================================
# Change these to switch default apps across all keybindings.

$terminal    = kitty
$fileManager = dolphin
$menu        = wofi --show drun

# Modifier key used in all keybindings (SUPER = Win key)
$mainMod = SUPER
